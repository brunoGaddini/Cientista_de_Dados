# Numpy = É uma biblioteca muito utilizada na área de ciência de dados. Trata-se de uma matriz, com várias dimensões, normalmente em dados possui 02 dimensões.
# As lista em numpy possuem tamanho fico. E todos os elementos tem que ser do mesmo tipo
# Computação científica, facilita operações matemáticas.

import numpy as np

a = np.array([12,34,26,18,10])
print(a)
print(a.dtype)